// This file contains the initialState of the app container
// and store reducer.

// The initial state of the app.
export const initialState = {

    // This field determine if the app component is mounted or not.
    isComponentMounted: false
};